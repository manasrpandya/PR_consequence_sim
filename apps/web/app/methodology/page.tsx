import Link from "next/link";import {DottedBackground} from "../../components/dotted-background";
export default function Methodology(){return <main className="method-page"><DottedBackground subdued/><div className="narrow-wrap"><span className="eyebrow">Methodology · Builder Week prototype</span><h1>Predict consequences across code and delivery workflow.</h1><p className="lede">A pull request changes more than files. It can alter dependencies, invalidate test assumptions, destabilize CI, and create review bottlenecks. Static review alone does not model that full path to merge.</p><Method number="01" title="The engineering problem"><p>Reviewers must infer blast radius, test coverage, operational criticality, and delivery delay from fragmented tools. The simulator joins those signals into one evidence-backed analysis and makes the next engineering decision explicit.</p></Method><Method number="02" title="What the current baseline combines"><ul><li>Repository modules and dependency edges</li><li>Changed files and module criticality</li><li>Module-to-test mappings and test duration</li><li>Synthetic historical CI failure associations</li><li>Workflow events, reruns, and review state</li></ul><p>Deterministic graph traversal predicts impact. Inspectable scoring ranks tests and risk factors. Workflow heuristics estimate time-to-green, and centralized action weights compare counterfactual interventions. Numerical outputs do not come from a language model.</p></Method><Method number="03" title="Where the learned system goes next"><p>The intended latent-twin architecture encodes repository structure and workflow events into a fused state. A PR and candidate action act as interventions; dedicated heads predict impact, tests, calibrated risk, ETA, bottlenecks, and action value. The differentiator is unifying technical and workflow consequence prediction in one action-oriented system—not an unsupported claim of category novelty.</p><p>GPT-5.6 can turn a compact evidence pack into a grounded explanation. It does not generate numerical predictions or replace traceable evidence, and a labelled deterministic provider takes over when the API is unavailable.</p></Method><Method number="04" title="Current limitations"><p>This version uses one fictional repository, prepared PRs, synthetic history, module-level dependency paths, and heuristic calibration. Its evaluation verifies that the product and metrics pipeline work end to end; it does not establish production accuracy or research validity.</p></Method><Method number="05" title="Privacy and provenance"><p>Public GitHub analysis uses repository metadata and changed source text. Uploaded projects are processed in best-effort request-scoped temporary storage and are not executed. GPT explanations receive a compact evidence summary rather than the full repository. This prototype is not a production security product.</p><p>Application version: <code>{process.env.VERCEL_GIT_COMMIT_SHA?.slice(0,12)??"local-development"}</code>.</p></Method><div className="method-cta"><p>Inspect the baseline on three deliberately different PR profiles.</p><Link className="button primary" href="/demo">Explore live demo →</Link></div></div></main>}
function Method({number,title,children}:{number:string;title:string;children:React.ReactNode}){return <section className="method"><div><span>{number}</span><h2>{title}</h2></div><div>{children}</div></section>}
